"use strict";

let answerQuestion = prompt("Каково «официальное» название JavaScript?", "");
if (answerQuestion == "ECMAScript" || "esmascript") {
  alert("Верно!");
} else {
  alert("Не знаете? «ECMAScript»!");
}
